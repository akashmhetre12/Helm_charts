replicaCount: 1
image:
  tag: 788f5a3b # overwritten by Jenkins via --set image.tag=<IMAGE_TAG> and yq bump
  pullSecret: ecr-pull-secret # created per-namespace, see setup-kind.sh
ingress:
  enabled: true
  className: nginx
  host: uat.internal.myapp.com
  # matches Jenkinsfile smoke test: https://<UAT_NAMESPACE>.internal.myapp.com/healthz
